---
import { Icon } from "astro-icon/components";
import { SITE_URL } from "../consts";
import type { PostEntry } from "../lib/utils";
import { formatDate, slugifyTaxonomy } from "../lib/utils";

interface Props {
  post: PostEntry;
}

const { post } = Astro.props;
const fullUrl = `${SITE_URL}/posts/${post.slug}/`;

const rewardQRCodes = [
  {
    label: "微信",
    src: "/avatar.jpeg",
    borderColor: "rgb(62, 159, 80)",
  },
  {
    label: "支付宝",
    src: "/avatar.jpeg",
    borderColor: "rgb(66, 90, 239)",
  },
];
---

<div class="post-footer">
  {/* ── 作者信息卡片 ── */}
  <section class="author-card">
    <div class="copyright-badge">
      <Icon name="material-symbols:copyright-rounded" width="32" height="32" />
    </div>

    <div class="card-body">
      <h3 class="card-title">{post.data.title}</h3>
      <p class="card-url">{fullUrl}</p>

      <div class="meta-grid">
        <div class="meta-item">
          <span class="meta-label">文章作者</span>
          <span class="meta-value">克喵 Moretti</span>
        </div>
        <div class="meta-item">
          <span class="meta-label">发布时间</span>
          <span class="meta-value">{formatDate(post.data.date)}</span>
        </div>
        <div class="meta-item">
          <span class="meta-label">版权信息</span>
          <a
            href="https://creativecommons.org/licenses/by-nc-sa/4.0/deed.zh-hans"
            target="_blank"
            rel="noopener"
            class="meta-value copyright-link"
          >
            CC BY-NC-SA 4.0
          </a>
        </div>
      </div>
    </div>
  </section>

  {/* ── 标签 + 打赏 ── */}
  <section class="post-bottom">
    <div class="tags-list">
      {
        post.data.tags.map((tag) => (
          <a href={`/tags/${slugifyTaxonomy(tag)}/`} class="tag-chip">
            <span class="tag-name">#{tag}</span>
          </a>
        ))
      }
    </div>
    <button class="reward-btn" id="reward-open-btn">
      <Icon name="material-symbols:favorite-rounded" width="16" height="16" />
      <span>打赏</span>
    </button>
  </section>
</div>

{/* ── 打赏弹窗 ── */}
<div class="reward-overlay" id="reward-overlay">
  <div class="reward-mask" id="reward-mask"></div>
  <div class="reward-panel">
    <div class="panel-header">
      <h2>打赏中心</h2>
      <button class="close-btn" id="reward-close-btn" aria-label="关闭">
        <Icon name="material-symbols:close-rounded" width="20" height="20" />
      </button>
    </div>
    <div class="panel-content">
      <p class="reward-title">感谢你赐予我前进的力量</p>
      <div class="qr-group">
        {
          rewardQRCodes.map((qr) => (
            <div class="qr-item">
              <img
                src={qr.src}
                alt={qr.label}
                class="qr-image"
                style={`border-color: ${qr.borderColor}`}
                width="130"
                height="130"
              />
              <span class="qr-label">{qr.label}</span>
            </div>
          ))
        }
      </div>
      <a href="/about/" class="donor-link">
        <span class="donor-link-text">赞赏者名单</span>
        <span class="donor-link-desc">因为你们的支持让我意识到写文章的价值</span
        >
      </a>
    </div>
  </div>
</div>

<script is:inline>
  (function () {
    function bind() {
      const overlay = document.getElementById("reward-overlay");
      const openBtn = document.getElementById("reward-open-btn");
      const closeBtn = document.getElementById("reward-close-btn");
      const mask = document.getElementById("reward-mask");
      if (!overlay || !openBtn || !closeBtn || !mask) return;
      if (openBtn.dataset.bound === "1") return;

      function open() {
        overlay.classList.add("active");
        document.body.style.overflow = "hidden";
      }
      function close() {
        overlay.classList.remove("active");
        document.body.style.overflow = "";
      }

      openBtn.addEventListener("click", open);
      closeBtn.addEventListener("click", close);
      mask.addEventListener("click", close);
      document.addEventListener("keydown", function (e) {
        if (e.key === "Escape" && overlay.classList.contains("active")) close();
      });

      openBtn.dataset.bound = "1";
    }

    if (document.readyState === "loading") {
      document.addEventListener("DOMContentLoaded", bind, { once: true });
    } else {
      bind();
    }
    document.addEventListener("astro:page-load", bind);
  })();
</script>

<style>
  /* ── 整体容器 ── */
  .post-footer {
    margin-top: 2.5rem;
    border: 1px solid rgb(228 228 231 / 0.8);
    border-radius: 0.75rem;
    background-color: rgb(255 255 255 / 0.8);
    backdrop-filter: blur(4px);
    overflow: hidden;
  }

  :global(html.dark) .post-footer {
    border-color: rgb(63 63 70 / 0.8);
    background-color: rgb(39 39 42 / 0.75);
  }

  /* ── 作者信息卡片 ── */
  .author-card {
    position: relative;
    padding: 1.5rem;
  }

  .copyright-badge {
    position: absolute;
    top: 1.5rem;
    right: 1.5rem;
    color: rgb(212 212 216);
    opacity: 0.5;
  }

  :global(html.dark) .copyright-badge {
    color: rgb(63 63 70);
  }

  .card-body {
    display: flex;
    flex-direction: column;
    gap: 0.75rem;
  }

  .card-title {
    font-size: 1.05rem;
    font-weight: 600;
    line-height: 1.4;
    color: rgb(24 24 27);
    margin: 0;
  }

  :global(html.dark) .card-title {
    color: rgb(228 228 231);
  }

  .card-url {
    font-size: 0.8rem;
    color: rgb(161 161 170);
    word-break: break-all;
    margin: 0;
  }

  :global(html.dark) .card-url {
    color: rgb(113 113 122);
  }

  .meta-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 0.75rem;
    margin-top: 0.25rem;
  }

  @media (max-width: 640px) {
    .meta-grid {
      grid-template-columns: 1fr 1fr;
    }
  }

  .meta-item {
    display: flex;
    flex-direction: column;
    gap: 0.15rem;
  }

  .meta-label {
    font-size: 0.75rem;
    font-weight: 500;
    color: rgb(113 113 122);
  }

  :global(html.dark) .meta-label {
    color: rgb(161 161 170);
  }

  .meta-value {
    font-size: 0.85rem;
    font-weight: 500;
    color: rgb(24 24 27);
    word-break: break-word;
  }

  :global(html.dark) .meta-value {
    color: rgb(228 228 231);
  }

  a.copyright-link {
    color: rgb(37 99 235);
    text-decoration: none;
  }

  :global(html.dark) a.copyright-link {
    color: rgb(147 197 253);
  }

  a.copyright-link:hover {
    text-decoration: underline;
  }

  /* ── 标签 + 打赏 ── */
  .post-bottom {
    display: flex;
    align-items: center;
    justify-content: space-between;
    flex-wrap: wrap;
    gap: 0.75rem;
    padding: 1rem 1.5rem;
    border-top: 1px solid rgb(228 228 231 / 0.8);
  }

  :global(html.dark) .post-bottom {
    border-top-color: rgb(63 63 70 / 0.8);
  }

  .tags-list {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
  }

  .tag-chip {
    display: inline-flex;
    align-items: center;
    height: 2rem;
    padding: 0 0.65rem;
    border-radius: 0.5rem;
    border: 1px solid rgb(228 228 231 / 0.8);
    background-color: rgb(250 250 250);
    font-size: 0.8rem;
    color: rgb(63 63 70);
    text-decoration: none;
    white-space: nowrap;
    transition:
      border-color 0.2s,
      background-color 0.2s;
  }

  :global(html.dark) .tag-chip {
    border-color: rgb(63 63 70 / 0.8);
    background-color: rgb(39 39 42);
    color: rgb(212 212 216);
  }

  .tag-chip:hover {
    border-color: rgb(59 130 246 / 0.5);
    background-color: rgb(239 246 255);
  }

  :global(html.dark) .tag-chip:hover {
    border-color: rgb(96 165 250 / 0.5);
    background-color: rgb(30 58 138 / 0.25);
  }

  .reward-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.35rem;
    height: 2rem;
    padding: 0 0.9rem;
    border-radius: 0.5rem;
    border: 1px solid rgb(244 63 94 / 0.3);
    background-color: rgb(255 241 242);
    font-size: 0.8rem;
    font-weight: 500;
    color: rgb(225 29 72);
    cursor: pointer;
    transition:
      background-color 0.2s,
      border-color 0.2s,
      box-shadow 0.2s;
  }

  :global(html.dark) .reward-btn {
    border-color: rgb(190 18 60 / 0.4);
    background-color: rgb(136 19 55 / 0.2);
    color: rgb(251 113 133);
  }

  .reward-btn:hover {
    background-color: rgb(254 226 226);
    border-color: rgb(244 63 94 / 0.5);
    box-shadow: 0 2px 8px rgb(244 63 94 / 0.12);
  }

  :global(html.dark) .reward-btn:hover {
    background-color: rgb(136 19 55 / 0.35);
    border-color: rgb(251 113 133 / 0.5);
    box-shadow: 0 2px 8px rgb(251 113 133 / 0.1);
  }

  /* ── 打赏弹窗 ── */
  .reward-overlay {
    position: fixed;
    inset: 0;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    pointer-events: none;
    opacity: 0;
    transition: opacity 0.2s ease;
  }

  .reward-overlay.active {
    pointer-events: auto;
    opacity: 1;
  }

  .reward-mask {
    position: absolute;
    inset: 0;
    background-color: rgb(0 0 0 / 0.2);
    backdrop-filter: blur(4px);
  }

  .reward-panel {
    position: relative;
    width: 92%;
    max-width: 420px;
    border-radius: 1rem;
    border: 1px solid rgb(228 228 231 / 0.8);
    background-color: rgb(255 255 255);
    box-shadow:
      0 10px 25px -5px rgb(0 0 0 / 0.1),
      0 8px 10px -6px rgb(0 0 0 / 0.06);
    padding: 1.5rem;
    transform: translateY(20px);
    transition: transform 0.2s ease;
  }

  .reward-overlay.active .reward-panel {
    transform: translateY(0);
  }

  :global(html.dark) .reward-panel {
    border-color: rgb(63 63 70 / 0.8);
    background-color: rgb(39 39 42);
    box-shadow:
      0 10px 25px -5px rgb(0 0 0 / 0.3),
      0 8px 10px -6px rgb(0 0 0 / 0.2);
  }

  .panel-header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 1.25rem;
  }

  .panel-header h2 {
    margin: 0;
    font-size: 1.1rem;
    font-weight: 600;
    color: rgb(24 24 27);
  }

  :global(html.dark) .panel-header h2 {
    color: rgb(228 228 231);
  }

  .close-btn {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    padding: 0.35rem;
    border: none;
    border-radius: 0.5rem;
    background-color: transparent;
    color: rgb(113 113 122);
    cursor: pointer;
    transition:
      background-color 0.15s,
      color 0.15s;
  }

  .close-btn:hover {
    background-color: rgb(244 244 245);
    color: rgb(24 24 27);
  }

  :global(html.dark) .close-btn:hover {
    background-color: rgb(63 63 70);
    color: rgb(228 228 231);
  }

  .panel-content {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 1rem;
  }

  .reward-title {
    font-size: 0.95rem;
    font-weight: 700;
    color: rgb(225 29 72);
    margin: 0;
  }

  .qr-group {
    display: flex;
    gap: 1.5rem;
  }

  .qr-item {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.35rem;
  }

  .qr-image {
    width: 130px;
    height: 130px;
    border-radius: 0.75rem;
    border: 2px solid;
    object-fit: cover;
  }

  .qr-label {
    font-size: 0.8rem;
    color: rgb(113 113 122);
  }

  :global(html.dark) .qr-label {
    color: rgb(161 161 170);
  }

  .donor-link {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.15rem;
    width: calc(100% - 1rem);
    padding: 0.75rem 1.5rem;
    border-radius: 0.75rem;
    border: 1px solid rgb(228 228 231 / 0.8);
    background-color: rgb(250 250 250);
    text-decoration: none;
    transition:
      background-color 0.2s,
      border-color 0.2s,
      box-shadow 0.2s;
  }

  :global(html.dark) .donor-link {
    border-color: rgb(63 63 70 / 0.8);
    background-color: rgb(39 39 42);
  }

  .donor-link:hover {
    border-color: rgb(244 63 94 / 0.5);
    background-color: rgb(255 241 242);
    box-shadow: 0 2px 8px rgb(244 63 94 / 0.1);
  }

  :global(html.dark) .donor-link:hover {
    border-color: rgb(251 113 133 / 0.5);
    background-color: rgb(136 19 55 / 0.25);
    box-shadow: 0 2px 8px rgb(251 113 133 / 0.08);
  }

  .donor-link-text {
    font-size: 0.9rem;
    font-weight: 700;
    color: rgb(24 24 27);
  }

  :global(html.dark) .donor-link-text {
    color: rgb(228 228 231);
  }

  .donor-link-desc {
    font-size: 0.75rem;
    color: rgb(161 161 170);
  }

  :global(html.dark) .donor-link-desc {
    color: rgb(113 113 122);
  }
</style>
